apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat-server-draft.fullname" . }}
  labels:
    {{- include "chat-server-draft.labels" . | nindent 4 }}
  annotations:
    {{ toYaml .Values.service.annotations | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: svchttp
  selector:
    {{- include "chat-server-draft.selectorLabels" . | nindent 4 }}
